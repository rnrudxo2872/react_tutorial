import React, {Component} from 'react';
import {data} from './listfile/topic.json';

class ReadTopic extends Component{
    render(){
        const results = data.map((data) => {
            
        })
        return(
            <h1></h1>
        );
    }
}

export default ReadTopic;